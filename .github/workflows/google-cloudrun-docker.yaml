name: Build and Deploy to Cloud Run

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v1
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: Setup gcloud
      uses: google-github-actions/setup-gcloud@v1
      with:
        project_id: painel-universidade

    - name: Configure Docker to use Artifact Registry
      run: |
        gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

    - name: Build and Push Docker Image
      run: |
        gcloud builds submit . \
          --tag us-central1-docker.pkg.dev/painel-universidade/painel-chips/painel-chips:latest

    - name: Deploy to Cloud Run
      run: |
        gcloud run deploy painel-chips \
          --image us-central1-docker.pkg.dev/painel-universidade/painel-chips/painel-chips:latest \
          --platform managed \
          --region us-central1 \
          --allow-unauthenticated
